{
  "name": "fdm-poc-app",
  "version": "1.0.0",
  "description": "FDM PoC App - Fastify + TypeScript + PostgreSQL",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "fastify": "^4.27.0",
    "pg": "^8.11.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/pg": "^8.11.5",
    "typescript": "^5.4.5",
    "ts-node-dev": "^2.0.0"
  }
}
